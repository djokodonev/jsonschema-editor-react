# Auth token is read from the GITHUB_PACKAGES_TOKEN env var so this file
# never carries a secret and stays safe to commit.
@djokodonev:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
