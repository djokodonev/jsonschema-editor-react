# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# OSX
.DS_Store

# compiled server output
# Keep /dist to allow for deployment from github
#dist

# storybook
storybook-static
.cache

yarn-error.log
/.idea/codeStyles/codeStyleConfig.xml
/.idea/codeStyles/Project.xml
/.idea/inspectionProfiles/profiles_settings.xml
/.idea/inspectionProfiles/Project_Default.xml
/.idea/.gitignore
/.idea/copilot.data.migration.agent.xml
/.idea/jsonschema-editor-react.iml
/.idea/misc.xml
/.idea/modules.xml
/.idea/vcs.xml

# Stray "> nul" / "2>nul" redirect captures from cmd.exe-style commands run in Cygwin/bash
nul
backend/nul

# Built Storybook static site (`npm run build-storybook` -o docs). Generated
# output, currently untracked; ignored so a build cannot be committed by
# accident. tsconfig.test.json excludes it for the same reason. (KAN-695)
docs/
